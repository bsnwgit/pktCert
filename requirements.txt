# pktCert Python dependencies

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.32

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.3.0

# App database
aiosqlite>=0.20.0

# Auth
python-jose[cryptography]>=3.4.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.2
httpx>=0.27.0
python3-saml>=1.16.0

# Notifications
aiosmtplib>=3.0.1
jinja2>=3.1.4

# Secret-at-rest encryption (Fernet) + X.509/CA operations
cryptography>=42.0.0
# CMS/PKCS#7 with custom authenticated attributes, for SCEP (RFC 8894).
# cryptography can encrypt, decrypt and sign PKCS#7, but its signature
# builder cannot attach the messageType/transactionID/nonce attributes SCEP
# carries inside signedData. Pure Python, no compiled extensions.
asn1crypto>=1.5.1

# Config
pyyaml>=6.0.1
python-dotenv>=1.0.0

